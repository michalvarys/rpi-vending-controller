# Host port where the hub dashboard is published. Inside the container it's always 8080.
PORT=8080

# How often (seconds) the hub refreshes state/logs from each RPi.
POLL_INTERVAL=3

# Per-request timeout when talking to an RPi.
POLL_TIMEOUT=4
